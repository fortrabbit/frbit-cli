version: 2

project_name: frbit

before:
  hooks:
    - go mod tidy

builds:
  - id: frbit
    main: ./cmd/frbit
    binary: frbit
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{ .Version }} -X main.commit={{ .Commit }} -X main.date={{ .Date }}

archives:
  - id: default
    ids:
      - frbit
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ if eq .Os \"darwin\" }}macOS_{{ if eq .Arch \"arm64\" }}AppleSilicon{{ else }}Intel{{ end }}{{ else }}{{ .Os }}_{{ .Arch }}{{ end }}"
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    files:
      - LICENSE
      - README.md
      - THIRD_PARTY_NOTICES

checksum:
  name_template: checksums.txt

sboms:
  - artifacts: archive
    disable: false

release:
  name_template: "{{ .ProjectName }} {{ .Version }}"
  draft: false
  prerelease: auto

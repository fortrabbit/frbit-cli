# @fortrabbit/cli

The npm distribution of the [frbit CLI](https://github.com/fortrabbit/frbit-cli).
It installs the native binary for the current operating system and architecture.

```sh
npm install --global @fortrabbit/cli
frbit --version
```

The platform packages are optional dependencies selected by npm. Do not install
with `--omit=optional`.
